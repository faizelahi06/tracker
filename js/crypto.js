const enc=new TextEncoder(), dec=new TextDecoder();
export const b64=b=>btoa(String.fromCharCode(...new Uint8Array(b))); export const unb64=s=>Uint8Array.from(atob(s),c=>c.charCodeAt(0));
export async function derive(pass,salt,iterations=210000){const km=await crypto.subtle.importKey('raw',enc.encode(pass),'PBKDF2',false,['deriveBits','deriveKey']);return crypto.subtle.deriveKey({name:'PBKDF2',hash:'SHA-256',salt,iterations},km,{name:'AES-GCM',length:256},false,['encrypt','decrypt'])}
export async function verifier(pass,salt,iterations=210000){const km=await crypto.subtle.importKey('raw',enc.encode(pass),'PBKDF2',false,['deriveBits']);return crypto.subtle.deriveBits({name:'PBKDF2',hash:'SHA-256',salt,iterations},km,256)}
export async function sha256(text){return [...new Uint8Array(await crypto.subtle.digest('SHA-256',enc.encode(text)))].map(x=>x.toString(16).padStart(2,'0')).join('')}
export async function encryptJSON(data,pass){const salt=crypto.getRandomValues(new Uint8Array(16)),iv=crypto.getRandomValues(new Uint8Array(12)),iterations=250000,key=await derive(pass,salt,iterations);const plain=enc.encode(JSON.stringify(data));const cipher=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,plain);return {schemaVersion:1,timestamp:new Date().toISOString(),algorithm:'AES-GCM',kdf:'PBKDF2-SHA-256',iterations,salt:b64(salt),iv:b64(iv),payload:b64(cipher),integrity:{cipherSha256:await sha256(b64(cipher))}}}
export async function decryptJSON(pkg,pass){if(pkg.schemaVersion!==1)throw Error('Unsupported backup version');const key=await derive(pass,unb64(pkg.salt),pkg.iterations);const plain=await crypto.subtle.decrypt({name:'AES-GCM',iv:unb64(pkg.iv)},key,unb64(pkg.payload));return JSON.parse(dec.decode(plain))}
