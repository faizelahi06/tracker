export function barChart(canvas,data,{max=100}={}){draw(canvas,(ctx,w,h)=>{const pad={l:110,r:20,t:20,b:25},cw=w-pad.l-pad.r,ch=h-pad.t-pad.b;ctx.font='11px system-ui';data.forEach((d,i)=>{const y=pad.t+i*(ch/data.length)+4,bh=Math.max(6,ch/data.length-8);ctx.fillStyle='#e6edf3';ctx.fillRect(pad.l,y,cw,bh);ctx.fillStyle='#3f8f62';ctx.fillRect(pad.l,y,cw*Math.min(1,d.value/max),bh);ctx.fillStyle='#243b53';ctx.fillText(trim(d.label,18),8,y+bh-2);ctx.fillText(Math.round(d.value)+'%',pad.l+cw-32,y+bh-2)})})}
export function lineChart(canvas,data){draw(canvas,(ctx,w,h)=>{const p=28,vals=data.map(d=>d.value),max=Math.max(1,...vals),cw=w-p*2,ch=h-p*2;ctx.strokeStyle='#d9e2ec';for(let i=0;i<4;i++){const y=p+i*ch/3;ctx.beginPath();ctx.moveTo(p,y);ctx.lineTo(w-p,y);ctx.stroke()}ctx.strokeStyle='#2f6b9a';ctx.lineWidth=2;ctx.beginPath();data.forEach((d,i)=>{const x=p+i*cw/Math.max(1,data.length-1),y=h-p-d.value/max*ch;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke();ctx.fillStyle='#627d98';ctx.font='10px system-ui';for(let i=0;i<data.length;i+=Math.max(1,Math.floor(data.length/6)))ctx.fillText(data[i].label,p+i*cw/Math.max(1,data.length-1)-12,h-8)})}
export function donutChart(canvas,parts){draw(canvas,(ctx,w,h)=>{const total=parts.reduce((a,b)=>a+b.value,0)||1,cx=w/2,cy=h/2,r=Math.min(w,h)*.32,colors=['#2f6b9a','#3f8f62','#d99b2b','#7057a3','#d66a3a','#4c9fbe'];let a=-Math.PI/2;parts.forEach((p,i)=>{const n=a+p.value/total*Math.PI*2;ctx.beginPath();ctx.strokeStyle=colors[i%colors.length];ctx.lineWidth=r*.38;ctx.arc(cx,cy,r,a,n);ctx.stroke();a=n});ctx.fillStyle='#243b53';ctx.font='700 22px system-ui';ctx.textAlign='center';ctx.fillText(Math.round(total),cx,cy+7);ctx.textAlign='start'})}
function draw(c,fn){const dpr=devicePixelRatio||1,r=c.getBoundingClientRect();c.width=r.width*dpr;c.height=r.height*dpr;const x=c.getContext('2d');x.scale(dpr,dpr);x.clearRect(0,0,r.width,r.height);fn(x,r.width,r.height)}function trim(s,n){return s.length>n?s.slice(0,n-1)+'…':s}
